# Portfolio screenshots

Add sanitized images here (e.g. `figma-button.png`, `docs-tabs.png`) and reference them from case studies or `PortfolioFigure` as needed.

Do not commit confidential client branding if policy forbids it — blur or use internal demo data.
